$(document).ready(function () {
    // Suponiendo que obtienes el nombre de usuario de alguna fuente, por ejemplo, una variable llamada nombreUsuario
    var nombreUsuario = "UsuarioEjemplo";

    // Actualizar el contenido del elemento h2 con el id "client-name" con el nombre de usuario obtenido
    $('#client-name').text(nombreUsuario);
})